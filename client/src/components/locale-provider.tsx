'use client';

// LocaleProvider - renders nothing, locale logic is handled by next-intl
// Language switcher is integrated in the app layout header
export default function LocaleProvider() {
  return null;
}
